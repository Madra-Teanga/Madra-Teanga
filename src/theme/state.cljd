(ns theme.state)

;; The single source of truth for theme state.
;;
;; :mode – the active display mode, either :light or :dark.
;;
;; This atom is watched by routing.root/view, so any change triggers
;; a full rebuild of the MaterialApp with updated ThemeData.
(def state
  (atom {:mode :light}))
